{
  "name": "james-endurance-plan",
  "private": true,
  "version": "2026.9.0",
  "description": "James Endurance Plan 2026 — zero-build PWA with Vercel serverless Strava sync",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
